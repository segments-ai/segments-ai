# General
numpy==1.19.5 # Change to >= 1.20 if we drop python 3.7 support (numpy 1.20+ includes type stubs).
requests==2.26.0
Pillow==9.1.0
scikit-image==0.19.2
tqdm==4.64.0
pycocotools;platform_system!="Windows"
pycocotools-windows;platform_system=="Windows"

# Type hints
typing_extensions
pydantic==1.9.0
types-Pillow
types-requests==2.27.25 